# 后端、账号权限与自检报告

检查日期：2026-09-02（Asia/Shanghai）

## 结论

本轮运行了 15 个金额/日期纯逻辑断言和 275 个后端账号、权限与业务断言，结果均为 `PASS`。

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `npm run self-check:logic`：15 个断言通过。
- `npm run self-check:backend`：275 个断言通过。
- `npm audit --omit=dev`：生产依赖 0 个已知漏洞。完整开发依赖扫描仍报告 Drizzle Kit 内部旧版 esbuild 加载器的 4 个中等风险告警；它只用于本地生成迁移，强制修复会把 Drizzle Kit 降级到不兼容版本，因此未采用。
- D1 抽查：首个账号为 `tabitoadimin01@tabitoedu.com` 且角色为 `admin`；密码列为 `pbkdf2$120000$...`；日元 5,000 已通过、人民币 8,000 已驳回，独立存储与汇总。
- 部门抽查：5 个默认选项有效；测试部门软停用后，旧申报仍保留原名称快照。

## 权限矩阵

| 能力 | 员工 | 审核员 | 管理员 |
|---|---:|---:|---:|
| 查看/修改本人资料 | 是 | 是 | 是 |
| 申报本人工资 | 是 | 是 | 是 |
| 修改已提交/已审核记录 | 否 | 否 | 否 |
| 查看所有员工已提交记录 | 否 | 是 | 是 |
| 通过/驳回工资 | 否 | 是 | 是 |
| 读取本人附件 | 是 | 是 | 是 |
| 读取他人证件、银行和工资附件 | 否 | 是 | 是 |
| 06 员工管理 | 否 | 是 | 是 |
| 查看月度工资汇总和受权 PDF | 否 | 是 | 是 |
| 导出月度工资汇总纯文本字段为 Excel | 否 | 是 | 是 |
| 最近 10 条操作 | 否 | 是 | 是 |
| 07 总审计和账号+月份追踪 | 否 | 是 | 是 |
| 管理角色、状态、会话和密码 | 否 | 否 | 是 |
| 增加/停用工作所属部门 | 否 | 否 | 是 |
| 授予/取消工作负责人权限 | 否 | 否 | 是 |
| 查看管理员全量审计 API | 否 | 否 | 是 |

所有判断都在 `app/lib/server/payroll-store.ts` 重新执行，隐藏前端按钮不是权限边界。

## 空数据库与账号

- 公开的 `/api/bootstrap-status` 只返回是否需要初始化以及固定账号名，不返回密码或其他数据。
- 空 D1 中首次调用注册 API 时，服务端忽略伪造的其他邮箱，固定创建 `TabitoAdimin01@tabitoedu.com` 的规范化值并授予管理员角色。
- 仓库不包含生产默认密码；首次部署者在注册页自行设置。
- 后续注册账号只获得员工角色；新账号注册默认开放，管理员仍可用注册开关关闭。
- 所有新账号必须先提交中文姓名、现住址和多行联系方式。进入工资功能前还会逐项检查四位年份生日、身份证件类型、收款方式、银行卡正反面 1–2 个附件以及对应收款字段；页面常驻提示、页内错误和弹窗会列出确切缺项，直接请求业务 API 也会收到 428。
- 登录身份证件、资格外活动许可和抚养资料是选填，不参与工资功能门禁。

## 工资状态、部门和币种

```text
草稿(1) -> 员工提交 -> 待审核(2) -> 通过(3)
                                      -> 驳回(4)
```

- 新建/修改时服务端强制草稿状态，重算工时和金额，并清除伪造审核字段。
- 新申报只能选择有效的动态部门。服务端不信任客户端传入的部门名，而是从 D1 读取后写入 `departmentLabel` 快照。
- 停用部门不会删除或改写旧申报；停用后不能用于新申报。
- `currency` 只允许 `JPY` 或 `CNY`，数据表有单独币种列，JSON 业务快照也保留币种。
- 待审、通过和驳回在按工作日期的月份中继续可查；自检确认处理后两种状态都仍在审批数据中。
- 月度提交只把所选月份的草稿改为待审核，其他月份草稿保持未提交。
- 时薪项目使用员工填写的休息小时/分钟从总时长扣除；金额以实际分钟计算，不会因先四舍五入小数小时产生偏差。服务端拒绝休息时间超过总时长的请求，并确认长工时不会被自动扣除休息。
- 服务端拒绝不存在的工作日期、未来生日以及超过页面限制的资料/申报文本，不再静默截断输入。
- 新申报的负责人必须来自当前启用的工作负责人账号；服务端以账号 ID 解析权威姓名，拒绝伪造或已停用选项。旧申报保留当时的姓名快照。管理员可以移除最后一个负责人权限，系统不会静默恢复；重新授予前将无法新建申报。
- 07 总审计仅把状态 3 定义为已审批总支出，并将 JPY/CNY 分开返回；没有汇率时不相加。选择账号后，月度、年度和部门统计均排除其他员工记录。

## 附件、审计与会话

- R2 保存文件字节，D1 保存所有者、元数据和资料/银行/工资引用。
- 员工只能读取本人附件；审核员和管理员可读取员工全部附件，跨账号读取会写入 `file.read_privileged`。
- 附件仍被资料或工资引用时不能删除。
- 最近审计 API 对审核员/管理员最多返回 10 条。账号追踪同时匹配账号本人的操作、对账号的操作、其工资和其文件；显示时优先使用资料中的注册姓名。
- 月度工资汇总只对审核员/管理员开放，包含员工联系方式、收款资料、按币种已审批金额与全部 PDF 下载引用；自检确认员工账号无法读取，Excel 导出不包含附件内容。
- 会话是 24 小时 `HttpOnly` + `SameSite=Strict` Cookie，D1 只存会话令牌 SHA-256 摘要。
- 连续 5 次错误密码锁定 15 分钟；管理员重置密码可解除锁定并撤销旧会话。

## 自检命令

```bash
npm run self-check:logic

PAYROLL_LOCAL_STATE_PATH=.local/backend-audit-20260902-final npm run dev -- --port 3200

# 另一终端
PAYROLL_TEST_BASE_URL=http://localhost:3200 \
PAYROLL_TEST_ADMIN_PASSWORD='set-a-test-password' \
npm run self-check:backend
```

脚本会写入测试账号、附件和工资，只能用于一次性本地数据库或完全隔离的测试环境，不能在生产 D1/R2 上运行。

## 运营边界

- 忘记密码邮件尚未接入邮件服务商；当前由管理员设置临时密码。
- 本系统记录资料、申报、审批和审计，不自动执行银行打款、税务申报或会计总账入账。
- 生产上线后需要由公司设定 D1/R2 备份、保留期、离职员工数据处理和管理员 MFA 规则。
